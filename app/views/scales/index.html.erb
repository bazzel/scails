<ul
  class="
    flex flex-col divide-y divide-gray-200 sm:gap-2 sm:divide-y-0
    dark:divide-gray-600
  "
>
  <%= render partial: "scale",
  collection: @scales,
  locals: {
    chromatic_scale: @chromatic_scale,
    pattern_name: @pattern_name,
    loop: @loop,
    range: @range,
    tempo: @tempo,
  } %>
</ul>
